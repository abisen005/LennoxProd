({
	handleInit: function handleInit(cmp, evt, h) {
		h.getAsssessment(cmp);
	},
	handleUserIdChange: function handleUserIdChange(cmp, evt, h) {
		h.getAsssessment(cmp);
	},
	handleInitIdChange: function handleInitIdChange(cmp, evt, h) {
		h.getAsssessment(cmp);
	}
});